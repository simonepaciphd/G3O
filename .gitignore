# Secrets
.env
.env.txt
.env.local

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Pipeline runtime artifacts (cache, ad-hoc outputs)
cache/
runs/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code local session state (ra-mode marker)
.claude/.ra-session.json
# Per-machine harness settings (permissions, local tool allowlists). Shared
# project config belongs in .claude/settings.json; the .local.json overlay is
# per-developer and must never be committed.
.claude/settings.local.json
